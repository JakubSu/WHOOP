meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/users/logout/
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "refresh": "{{refreshToken}}"
  }
}
