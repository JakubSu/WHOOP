info:
  name: "List Plan Workouts"
  type: "http"
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/training-plans/{{trainingPlanId}}/workouts/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
