info:
  name: "Get Training Plan"
  type: "http"
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/training-plans/{{trainingPlanId}}/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
