info:
  name: "List Training Plans"
  type: "http"
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/training-plans/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
