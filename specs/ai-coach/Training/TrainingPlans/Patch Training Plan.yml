info:
  name: "Patch Training Plan"
  type: "http"
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/training-plans/{{trainingPlanId}}/"
  headers:
    - name: "Content-Type"
      value: "application/json"
  body:
    type: "json"
    data: |-
      {
        "name": "Updated Summer Block"
      }
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
