info:
  name: "List Workout Exercises Page"
  type: "http"
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/workouts/{{workoutId}}/exercises/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
