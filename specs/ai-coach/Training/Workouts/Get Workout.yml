info:
  name: "Get Workout"
  type: "http"
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/workouts/{{workoutId}}/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
