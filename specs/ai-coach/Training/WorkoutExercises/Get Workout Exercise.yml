info:
  name: "Get Workout Exercise"
  type: "http"
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/workout-exercises/{{workoutExerciseId}}/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
