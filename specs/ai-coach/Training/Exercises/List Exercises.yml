info:
  name: "List Exercises"
  type: "http"
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/exercises/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
