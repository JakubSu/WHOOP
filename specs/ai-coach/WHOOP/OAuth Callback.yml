info:
  name: "OAuth Callback"
  type: "http"
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/whoop/callback/?code={{whoopOAuthCode}}&state={{whoopOAuthState}}"
  auth: "none"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
