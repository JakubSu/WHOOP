info:
  name: "Get Connect URL"
  type: "http"
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/whoop/connect-url/?success_url={{frontendSuccessUrl}}"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
