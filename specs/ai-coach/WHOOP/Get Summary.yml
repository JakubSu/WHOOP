info:
  name: "Get Summary"
  type: "http"
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/whoop/summary/"
  auth: "inherit"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
