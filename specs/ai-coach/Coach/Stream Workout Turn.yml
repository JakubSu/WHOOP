info:
  name: Stream Workout Turn
  type: http
  seq: 1
  tags:
    - Coach

http:
  method: POST
  url: "{{baseUrl}}/coach/turns/stream/"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: text/event-stream
  body:
    type: json
    data: |-
      {
        "page_context": {
          "page_type": "workout",
          "context_id": "{{workoutId}}"
        },
        "message": "What should I adjust in this workout based on my recent recovery?"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: SSE Response
    description: Creates or reuses a workout coach conversation and may create a recommendation.
    request:
      url: "{{baseUrl}}/coach/turns/stream/"
      method: POST
      headers:
        - name: Content-Type
          value: application/json
        - name: Accept
          value: text/event-stream
      body:
        type: json
        data: |-
          {
            "page_context": {
              "page_type": "workout",
              "context_id": "{{workoutId}}"
            },
            "message": "What should I adjust in this workout based on my recent recovery?"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: text/event-stream
      body:
        type: text
        data: |-
          event: conversation_started
          data: {"conversation_id":"{{coachConversationId}}","page_context":{"page_type":"workout","context_id":"{{workoutId}}"}}

          event: assistant_done
          data: {"message":{"id":"{{coachMessageId}}","role":"assistant","content":"..."}}
