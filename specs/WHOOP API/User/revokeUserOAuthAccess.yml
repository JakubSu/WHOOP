info:
  name: revokeUserOAuthAccess
  type: http
  seq: 3
  tags:
    - User

http:
  method: DELETE
  url: "{{baseUrl}}/v2/user/access"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 Response
    description: Successful request; no response body
    request:
      url: "{{baseUrl}}/v2/user/access"
      method: DELETE
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
  - name: 400 Response
    description: Client error constructing the request
    request:
      url: "{{baseUrl}}/v2/user/access"
      method: DELETE
    response:
      status: 400
      statusText: Bad Request
      body:
        type: text
        data: ""
  - name: 401 Response
    description: Invalid authorization
    request:
      url: "{{baseUrl}}/v2/user/access"
      method: DELETE
    response:
      status: 401
      statusText: Unauthorized
      body:
        type: text
        data: ""
  - name: 429 Response
    description: Request rejected due to rate limiting
    request:
      url: "{{baseUrl}}/v2/user/access"
      method: DELETE
    response:
      status: 429
      statusText: Too Many Requests
      body:
        type: text
        data: ""
  - name: 500 Response
    description: Server error occurred while making request
    request:
      url: "{{baseUrl}}/v2/user/access"
      method: DELETE
    response:
      status: 500
      statusText: Internal Server Error
      body:
        type: text
        data: ""
